{{>userheader}}

<section id="shopgrid" class="shop shop-grid">
    <div class="container">

        <div class="container heading">

            <div class="row">
                <div class="col-xs-6 col-sm-6 col-md-6">
                    <p class="subheading" style="line-height: 12px;">EXPLORE MORE PRODUCTS FROM</p>
                    <h2>{{prodBrandData.Prod_Brand}}</h2>
                </div>

                <div class="col-xs-6 col-sm-6 col-md-6" style="display:flex;justify-content:end">
                    <img src="/product-brand-logos/{{prodBrandData._id}}PBL.jpg" style="height:80px;width:80px" alt="client">
                </div>

            </div>

        </div>


        <div class="row">

            {{#each products}}

            <div class="col-xs-12 col-sm-6 col-md-3 product">
                <div class="product-img">
                    <img src="/product-images/{{this._id}}_1.jpg" alt="Product" />
                    <div class="product-hover">
                        <div class="product-action">
                            {{#if ../user}}

                            <a class="btn btn-primary" href="/prodBuyNow?prodId={{this._id}}">Buy now</a>



                            {{#hello ../cart this._id this.Price this.Product_Name}}

                            {{/hello}}

                            {{else}}

                            <a class="btn btn-primary" href="/login">Buy now</a>


                            <a class="btn btn-primary" href="/login">Add to cart</a>

                            {{/if}}
                        </div>
                    </div>

                </div>

                <div class="product-bio">
                    <div class="prodcut-cat">
                        <a href="/shopByProdBrand?prodBrand={{this.Product_Brand}}">{{this.Product_Brand}}</a>
                    </div>

                    <div class="prodcut-title">
                        <h3>
                            <a href="/view-product?id={{this._id}}">{{this.Product_Name}}</a>
                        </h3>
                    </div>

                    <div class="product-price">
                        <span class="symbole">₹</span><span>{{this.Price}}</span>
                    </div>

                </div>

            </div>

            {{/each}}

        </div>

        {{!-- <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-12">
                <ul class="pagination">
                    <li class="active">
                        <a href="#">1</a>
                    </li>
                    <li>
                        <a href="#">2</a>
                    </li>
                    <li>
                        <a href="#">3</a>
                    </li>
                    <li>
                        <a href="#" aria-label="Next">
                            <span aria-hidden="true"><i class="fa fa-angle-right"></i></span>
                        </a>
                    </li>
                </ul>
            </div>

        </div> --}}
    </div>

</section>

{{>userfooter}}

<script>

    function addToCart(prodId, price, prodName) {
        $.ajax({

            url: '/add-to-cart?id=' + prodId + '&price=' + price + '&prodName=' + prodName,
            method: 'get',
            success: (response) => {

                if (response.status) {
                    let count = $('#cart-count').html()
                    count = parseInt(count) + 1
                    $("#cart-count").html(count)
                    swal("Success!", "Product added to cart!", "success", {
                        button: "OK",
                    }).then(() => {
                        location.reload()
                    })

                }

            }
        })
    }

</script>